namespace atividade {
    function multiplicacao(num1: number, num2: number): number {
        return num1 * num2
    }
    
    function olaNome(nome: string): string {
        return `Olá ${nome}`
    }
    
    console.log(multiplicacao(10, 20))
    console.log(olaNome('Yuri'))
}